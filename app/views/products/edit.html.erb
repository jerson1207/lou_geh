<div class="section">
  <div class="section-header">
    <%= link_to 'Back', products_path, class: "btn-back" %>
    <h1>Editing Product</h1>  
  </div>
  <%= render 'form', product: @product %>
</div>